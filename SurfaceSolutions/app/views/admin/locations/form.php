<h1>Formulär</h1><form method="post"><?=csrf_field()?><input name="name" value="<?=e($item['name'] ?? '')?>" placeholder="Namn"><input name="title" value="<?=e($item['title'] ?? '')?>" placeholder="Titel"><input name="slug" value="<?=e($item['slug'] ?? '')?>" placeholder="slug"><textarea name="content"><?=e($item['content'] ?? '')?></textarea><textarea name="short_description"><?=e($item['short_description'] ?? '')?></textarea><textarea name="intro"><?=e($item['intro'] ?? '')?></textarea><input name="meta_title" value="<?=e($item['meta_title'] ?? '')?>" placeholder="Meta title"><input name="meta_description" value="<?=e($item['meta_description'] ?? '')?>" placeholder="Meta description"><input name="status" value="<?=e($item['status'] ?? 'active')?>" placeholder="status"><button class="btn">Spara</button></form>
